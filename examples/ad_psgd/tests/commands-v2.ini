# Sample command file
# You can make this file as the stdin of conductor.py so that included commands will execute sequentially
# Example usage: python conductor.py < commands.ini
# (note: without path modification, this file cannot be used in other working directories)
# Lines that start with # (just like this one) are comments and will be ignored
configure configs/dev/appConfig-medium-v2.yaml
start --num_rounds 20